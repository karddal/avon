services:
  backend:
    build: .
    ports:
      - "8000:8000"
    environment:
      - JWT_SECRET_KEY="hihihihih"
      - ACCESS_TOKEN_EXPIRY_MINUTES=600
      - DATABASE_URL=sqlite:///database.db
      - CORS_ORIGIN=http://localhost:3000
